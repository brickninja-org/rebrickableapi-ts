{
  "name": "rebrickableapi",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test -ws",
    "release": "npm run build -ws && changeset publish"
  },
  "dependencies": {
    "@changesets/cli": "2.28.0"
  },
  "devDependencies": {
    "@changeset-bot/changelog": "0.0.4"
  }
}