{
  "name": "rebrickableapi",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test -ws",
    "release": "npm run build -ws && changeset publish"
  },
  "dependencies": {
    "@changesets/cli": "2.27.12"
  },
  "devDependencies": {
    "@changeset-bot/changelog": "0.0.4"
  }
}